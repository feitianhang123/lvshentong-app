
## 2026-04-20 22:40
- **强化记忆**：
  - 本次工作完成了 **绿申通（1.0.21).apk** 的生成，关键更新包括：
    - 将模块5中 “中国绿色食品发展中心” 链接改为 `onclick="window.open('http://www.greenfood.agri.cn/', '_system'); return false;"`，确保点击后在手机浏览器打开。
    - 更新 `apktool.yml` 中 `versionCode` 为 **4**（对应版本号递增），`versionName` 为 **1.0.21**，并保持文件名与版本号一致（`绿申通（1.0.21).apk`）。
    - 统一所有模块（包括主界面）底部灰色栏的高度、边距、文字居中方式，使 UI 保持一致。
    - 重新使用 **debug keystore**（v2 签名）完成签名。
  - **记忆规则**：
    - 每次更改后，必须在 `apktool.yml` 同步更新 `versionCode`（递增）和 `versionName`（递增的 x.y.z 形式），并在重建后使用相同的版本号生成对应命名的 APK（`绿申通（<versionName>).apk`）。
    - 链接打开方式需使用 `onclick="window.open('URL', '_system'); return false;"`，而非仅 `target="_blank"`。
    - 所有模块的灰色底部栏样式应保持统一：`position:fixed; bottom:0; height:60px; display:flex; align-items:center; justify-content:center; text-align:center;`，文字居中。
  - **防止重复工作**：在未来的工作流中，先检查 `memory/2026-04-20.md`，确认最新的版本号、链接实现方式以及 UI 统一样式，再进行新的改动。
